#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase in reverse
 *
 * Return: Always 0.
 */
int main(void)
{
			char letter;

			for (letter = 'z'; letter <= 'z'; letter--)
				putchar(letter);

			putchar('\n');

			return (0);
}
